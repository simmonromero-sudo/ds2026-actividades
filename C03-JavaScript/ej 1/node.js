const nombre = "Simón";
const apellido = "Romero";
const edad = 20;
const materia = "DS2026";

const mensaje = `Hola, mi nombre es ${nombre} ${apellido}, tengo ${edad} años y estoy cursando la materia ${materia}.`;
console.log(mensaje);

let contador = 0;

contador += 1;
contador += 1;
contador += 1;  

console.log(`El contador tiene el valor: ${contador}`);